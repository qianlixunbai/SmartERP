# テストドキュメント

> <span style="color: #888888;"># 测试文档</span>

## SmartOA 承認フロー管理システム

> <span style="color: #888888;">## SmartOA 审批流管理系统</span>

---

## 1. テスト概要

> <span style="color: #888888;">## 1. 测试概述</span>

| 項目 | 説明 |
|---|---|
| テストタイプ | 手動機能テスト + APIテスト |
| テスト環境 | Windows 11 + JDK 21 + MySQL 8 + Chrome |
| テストデータ | シードデータ：5ユーザー + 1テンプレート + 2承認ノード |

> <span style="color: #888888;">| 项 | 说明 |</span>
> <span style="color: #888888;">|---|---|</span>
> <span style="color: #888888;">| 测试类型 | 手工功能测试 + 接口测试 |</span>
> <span style="color: #888888;">| 测试环境 | Windows 11 + JDK 21 + MySQL 8 + Chrome |</span>
> <span style="color: #888888;">| 测试数据 | 种子数据：5 个用户 + 1 个模板 + 2 个审批节点 |</span>

---

## 2. テストアカウント

> <span style="color: #888888;">## 2. 测试账号</span>

| ユーザー名 | パスワード | ロール | 部署 | 直属上司 | 部署ディレクター |
|---|---|---|---|---|---|
| admin | 123456 | MANAGER | 技術部 | — | 張ディレクター |
| zhangsan | 123456 | EMPLOYEE | 技術部 | 王マネージャー(admin) | 張ディレクター(4) |
| lisi | 123456 | EMPLOYEE | 製品部 | 王マネージャー(admin) | 李ディレクター(5) |
| zongjian1 | 123456 | MANAGER | 技術部 | — | — |
| zongjian2 | 123456 | MANAGER | 製品部 | — | — |

> <span style="color: #888888;">| 用户名 | 密码 | 角色 | 部门 | 直属领导 | 部门总监 |</span>
> <span style="color: #888888;">|---|---|---|---|---|---|</span>
> <span style="color: #888888;">| admin | 123456 | MANAGER | 技术部 | — | 张总监 |</span>
> <span style="color: #888888;">| zhangsan | 123456 | EMPLOYEE | 技术部 | 王经理(admin) | 张总监(4) |</span>
> <span style="color: #888888;">| lisi | 123456 | EMPLOYEE | 产品部 | 王经理(admin) | 李总监(5) |</span>
> <span style="color: #888888;">| zongjian1 | 123456 | MANAGER | 技术部 | — | — |</span>
> <span style="color: #888888;">| zongjian2 | 123456 | MANAGER | 产品部 | — | — |</span>

---

## 3. 機能テストケース

> <span style="color: #888888;">## 3. 功能测试用例</span>

### 3.1 認証モジュール

> <span style="color: #888888;">### 3.1 认证模块</span>

| ケース番号 | テストシナリオ | 前提条件 | 操作手順 | 期待結果 |
|---|---|---|---|---|
| AUTH-01 | 正しいアカウントでログイン | データベースにユーザー登録済み | zhangsan / 123456 を入力 | ログイン成功、ホームページへ遷移、Token が localStorage に保存される |
| AUTH-02 | 誤ったパスワードでログイン | — | zhangsan / wrong を入力 | 「ユーザー名またはパスワードが間違っています」と表示 |
| AUTH-03 | 未ログインでアクセス | Token をクリア | /submit-application に直接アクセス | ログインページにリダイレクト |
| AUTH-04 | Token 期限切れ | Token を無効な値に変更 | 任意の /api/ インターフェースにリクエスト | 401 が返り、ログインページにリダイレクト |
| AUTH-05 | ログアウト | ログイン済み | ログアウトをクリック | Token と User がクリアされ、ログインページにリダイレクト |

> <span style="color: #888888;">| 用例编号 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |</span>
> <span style="color: #888888;">|---|---|---|---|---|</span>
> <span style="color: #888888;">| AUTH-01 | 正确账号登录 | 数据库已有用户 | 输入 zhangsan / 123456 | 登录成功，跳转首页，Token 存储到 localStorage |</span>
> <span style="color: #888888;">| AUTH-02 | 错误密码登录 | — | 输入 zhangsan / wrong | 提示"用户名或密码错误" |</span>
> <span style="color: #888888;">| AUTH-03 | 未登录访问 | 清除 Token | 直接访问 /submit-application | 跳转登录页 |</span>
> <span style="color: #888888;">| AUTH-04 | Token 过期 | 修改 Token 为无效值 | 请求任意 /api/ 接口 | 返回 401，跳转登录页 |</span>
> <span style="color: #888888;">| AUTH-05 | 登出 | 已登录 | 点击登出 | Token 和 User 清除，跳转登录页 |</span>

### 3.2 テンプレート管理モジュール

> <span style="color: #888888;">### 3.2 模板管理模块</span>

| ケース番号 | テストシナリオ | 前提条件 | 操作手順 | 期待結果 |
|---|---|---|---|---|
| TPL-01 | テンプレート一覧 | admin でログイン | テンプレート管理に入る | テンプレート一覧が表示される |
| TPL-02 | 新規テンプレート作成 | admin でログイン | 名前+説明を入力し、保存 | 作成成功、一覧が更新される |
| TPL-03 | テンプレート編集 | admin でログイン | 名前を変更し、保存 | 更新成功 |
| TPL-04 | テンプレート削除 | admin でログイン | 削除をクリックし、確認 | テンプレートが削除され、関連ノードとフィールドがカスケード削除される |
| TPL-05 | 承認ノード設定 | admin でログイン、テンプレート編集中 | ノード追加 → 承認者タイプ選択 → 保存 | ノード保存成功、一覧が更新される |
| TPL-06 | ノード削除 | admin でログイン | ノード削除ボタンをクリック | ノードが削除される |
| TPL-07 | 非管理者アクセス | zhangsan でログイン | /templates にアクセス | ホームページにリダイレクト、権限なし |
| TPL-08 | 承認モード設定（P2） | admin でログイン | ノードで「会签」を選択 + 承認者を複数選択 | 保存成功、approverIds がカンマ区切りで保存される |
| TPL-09 | 条件分岐設定（P2） | admin でログイン | ノードに `days > 3` を入力 | 保存成功、conditionExpression が永続化される |
| TPL-10 | タイムアウト設定（P2） | admin でログイン | ノードに 48h + AUTO_APPROVE を設定 | 保存成功、timeoutHours/timeoutAction が永続化される |

> <span style="color: #888888;">| 用例编号 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |</span>
> <span style="color: #888888;">|---|---|---|---|---|</span>
> <span style="color: #888888;">| TPL-01 | 模板列表 | admin 登录 | 进入模板管理 | 显示模板列表 |</span>
> <span style="color: #888888;">| TPL-02 | 新建模板 | admin 登录 | 填写名称+描述，保存 | 创建成功，列表刷新 |</span>
> <span style="color: #888888;">| TPL-03 | 编辑模板 | admin 登录 | 修改名称，保存 | 更新成功 |</span>
> <span style="color: #888888;">| TPL-04 | 删除模板 | admin 登录 | 点击删除，确认 | 模板删除，关联节点和字段级联删除 |</span>
> <span style="color: #888888;">| TPL-05 | 配置审批节点 | admin 登录，编辑模板 | 添加节点 → 选择审批人类型 → 保存 | 节点保存成功，列表刷新 |</span>
> <span style="color: #888888;">| TPL-06 | 删除节点 | admin 登录 | 点击节点删除按钮 | 节点移除 |</span>
> <span style="color: #888888;">| TPL-07 | 非管理员访问 | zhangsan 登录 | 访问 /templates | 跳转首页，无权限 |</span>
> <span style="color: #888888;">| TPL-08 | 配置签批模式（P2） | admin 登录 | 节点选择"会签"+ 多选审批人 | 保存成功，approverIds 逗号分隔存储 |</span>
> <span style="color: #888888;">| TPL-09 | 配置条件分支（P2） | admin 登录 | 节点填写 `days > 3` | 保存成功，conditionExpression 持久化 |</span>
> <span style="color: #888888;">| TPL-10 | 配置超时（P2） | admin 登录 | 节点设置 48h + AUTO_APPROVE | 保存成功，timeoutHours/timeoutAction 持久化 |</span>

### 3.3 申請提出モジュール

> <span style="color: #888888;">### 3.3 申请提交模块</span>

| ケース番号 | テストシナリオ | 前提条件 | 操作手順 | 期待結果 |
|---|---|---|---|---|
| LEV-01 | 正常提出 | zhangsan でログイン | テンプレート選択 → 休暇タイプ選択 → 日付選択 → 理由入力 → 提出 | 提出成功、一覧が更新される |
| LEV-02 | 必須項目バリデーション | zhangsan でログイン | 日付を選択せずに提出 | 必須項目のプロンプトが表示される |
| LEV-03 | 日付バリデーション | zhangsan でログイン | 終了日が開始日より前 | 「終了日は開始日より前にできません」と表示 |
| LEV-04 | 上司未割り当てで提出 | zongjian1 でログイン | 提出を試行（直属上司なし） | 「直属上司が未割り当てです。提出できません」と表示 |

> <span style="color: #888888;">| 用例编号 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |</span>
> <span style="color: #888888;">|---|---|---|---|---|</span>
> <span style="color: #888888;">| LEV-01 | 正常提交 | zhangsan 登录 | 选择模板 → 选择请假类型 → 选日期 → 填原因 → 提交 | 提交成功，列表刷新 |</span>
> <span style="color: #888888;">| LEV-02 | 必填校验 | zhangsan 登录 | 不选日期直接提交 | 提示必填项 |</span>
> <span style="color: #888888;">| LEV-03 | 日期校验 | zhangsan 登录 | 结束日期早于开始日期 | 提示"结束日期不能早于开始日期" |</span>
> <span style="color: #888888;">| LEV-04 | 无领导提交 | zongjian1 登录 | 尝试提交（无直属领导） | 提示"尚未分配直属领导，无法提交" |</span>

### 3.4 承認フローモジュール

> <span style="color: #888888;">### 3.4 审批流转模块</span>

| ケース番号 | テストシナリオ | 前提条件 | 操作手順 | 期待結果 |
|---|---|---|---|---|
| APP-01 | SINGLE 承認通過 | zhangsan が申請を提出 | admin でログイン → 承認待ち一覧 → 通過 | ステータス PENDING、次のノードに遷移 |
| APP-02 | 最終承認通過 | 前のノードが通過済み | 最終承認者が通過 | ステータスが APPROVED に変わる |
| APP-03 | 承認却下 | 承認待ち項目あり | 承認者がログイン → 却下 | ステータスが REJECTED に変わり、並列タスクは SKIPPED |
| APP-04 | 非承認者による操作 | 承認待ち項目が admin に属する | zhangsan がログインして承認を試行 | 操作権限なしと表示 |
| APP-05 | 重複承認 | 申請が既に通過済み | 再度承認 | 「この休暇申請は既に処理されました」と表示 |

> <span style="color: #888888;">| 用例编号 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |</span>
> <span style="color: #888888;">|---|---|---|---|---|</span>
> <span style="color: #888888;">| APP-01 | SINGLE 审批通过 | zhangsan 提交申请 | admin 登录 → 待审批列表 → 通过 | 状态 PENDING，流转到下一节点 |</span>
> <span style="color: #888888;">| APP-02 | 最终审批通过 | 上一节点已通过 | 最终审批人通过 | 状态变为 APPROVED |</span>
> <span style="color: #888888;">| APP-03 | 审批驳回 | 有待审批项 | 审批人登录 → 驳回 | 状态变为 REJECTED，并行任务 SKIPPED |</span>
> <span style="color: #888888;">| APP-04 | 非审批人操作 | 待审批项属于 admin | zhangsan 登录尝试审批 | 提示无权操作 |</span>
> <span style="color: #888888;">| APP-05 | 重复审批 | 申请已通过 | 再次审批 | 提示"该请假单已处理" |</span>

### 3.5 撤回と転送モジュール

> <span style="color: #888888;">### 3.5 撤回与转派模块</span>

| ケース番号 | テストシナリオ | 前提条件 | 操作手順 | 期待結果 |
|---|---|---|---|---|
| WIT-01 | 申請者による撤回 | zhangsan の PENDING 申請 | 詳細に入る → 撤回をクリック | ステータスが WITHDRAWN に変わる |
| WIT-02 | 却下済みは撤回不可 | 申請が既に却下済み | — | 撤回ボタンが表示されない |
| WIT-03 | 他者による撤回不可 | lisi が zhangsan の申請を閲覧 | — | 撤回ボタンが表示されない |
| TRF-01 | SINGLE 転送 | admin に承認待ち項目あり | 転送をクリック → ユーザー選択 → 確定 | 承認者が変更され、転送記録が残る |
| TRF-02 | 並列ノードでの転送禁止 | 申請が会签ノードにある | — | 転送ボタンが表示されない、または非対応と表示 |

> <span style="color: #888888;">| 用例编号 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |</span>
> <span style="color: #888888;">|---|---|---|---|---|</span>
> <span style="color: #888888;">| WIT-01 | 申请人撤回 | zhangsan 的 PENDING 申请 | 进入详情 → 点击撤回 | 状态变为 WITHDRAWN |</span>
> <span style="color: #888888;">| WIT-02 | 已驳回无法撤回 | 申请已驳回 | — | 撤回按钮不显示 |</span>
> <span style="color: #888888;">| WIT-03 | 他人无法撤回 | lisi 查看 zhangsan 的申请 | — | 撤回按钮不显示 |</span>
> <span style="color: #888888;">| TRF-01 | SINGLE 转派 | admin 有待审批项 | 点击转派 → 选择用户 → 确定 | 审批人变更，留转派记录 |</span>
> <span style="color: #888888;">| TRF-02 | 并行节点禁止转派 | 申请在会签节点 | — | 转派按钮不显示或提示不支持 |</span>

### 3.6 並列承認モジュール（P2）

> <span style="color: #888888;">### 3.6 并行审批模块（P2）</span>

| ケース番号 | テストシナリオ | 前提条件 | 操作手順 | 期待結果 |
|---|---|---|---|---|
| PAR-01 | 会签全員同意 | 2人会签テンプレートで申請提出 | 1人目が通過 → 2人目が通過 | 2人目の通過後に次のノードへ遷移 |
| PAR-02 | 会签1人目が通過 | 同上 | 1人目が通過 | ステータスは PENDING のまま、そのタスクのみ COMPLETED |
| PAR-03 | 会签で誰かが拒否 | 同上 | 1人目が通過 → 2人目が拒否 | リクエストは REJECTED、1人目のタスクは COMPLETED のまま |
| PAR-04 | 或签いずれかが同意 | 2人或签テンプレートで申請提出 | 1人目が通過 | 即座に遷移、2人目のタスクは SKIPPED |
| PAR-05 | 或签拒否 | 2人或签テンプレート | 1人目が拒否 | リクエストは REJECTED、2人目のタスクは SKIPPED |
| PAR-06 | 並列ノード撤回 | 申請者が会签中 | 申請者が撤回 | ステータス WITHDRAWN、全 PENDING タスクが SKIPPED |
| PAR-07 | 混合パイプライン | SINGLE → COUNTER_SIGN テンプレート | 第1ノード通過 → 会签全員同意 | 正常に遷移、最終的に APPROVED |

> <span style="color: #888888;">| 用例编号 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |</span>
> <span style="color: #888888;">|---|---|---|---|---|</span>
> <span style="color: #888888;">| PAR-01 | 会签全部同意 | 2人会签模板提交申请 | 第一人通过 → 第二人通过 | 第二人通过后推进到下一节点 |</span>
> <span style="color: #888888;">| PAR-02 | 会签第一人通过 | 同上 | 第一人通过 | 状态保持 PENDING，仅该任务 COMPLETED |</span>
> <span style="color: #888888;">| PAR-03 | 会签有人拒绝 | 同上 | 第一人通过 → 第二人拒绝 | 请求 REJECTED，第一人任务保持 COMPLETED |</span>
> <span style="color: #888888;">| PAR-04 | 或签任一同意 | 2人或签模板提交申请 | 第一人通过 | 立即推进，第二人任务 SKIPPED |</span>
> <span style="color: #888888;">| PAR-05 | 或签拒绝 | 2人或签模板 | 第一人拒绝 | 请求 REJECTED，第二人任务 SKIPPED |</span>
> <span style="color: #888888;">| PAR-06 | 并行节点撤回 | 申请人在会签中 | 申请人撤回 | 状态 WITHDRAWN，所有 PENDING 任务 SKIPPED |</span>
> <span style="color: #888888;">| PAR-07 | 混合流水线 | SINGLE → COUNTER_SIGN 模板 | 第一节点通过 → 会签全部同意 | 正常流转，最终 APPROVED |</span>

### 3.7 タイムアウト自動エスカレーションモジュール（P2）

> <span style="color: #888888;">### 3.7 超时自动升级模块（P2）</span>

| ケース番号 | テストシナリオ | 前提条件 | 操作手順 | 期待結果 |
|---|---|---|---|---|
| TMO-01 | タイムアウト転送 | ノードに ESCALATE + ターゲットユーザー設定 | timeoutTime の期限切れを待つ | 自動的に escalateToUserId に転送される |
| TMO-02 | タイムアウト自動通過 | ノードに AUTO_APPROVE 設定 | timeoutTime の期限切れを待つ | 自動通過、並列タスクをスキップして遷移 |
| TMO-03 | タイムアウト自動却下 | ノードに AUTO_REJECT 設定 | timeoutTime の期限切れを待つ | 自動却下、TIMEOUT_REJECT を記録 |
| TMO-04 | タイムアウト設定なし | ノードにタイムアウト未設定 | 長期 PENDING | タイムアウト処理がトリガーされない |
| TMO-05 | タイムアウト記録 | タイムアウトがトリガーされる | 承認記録を確認 | approverId=0, action=TIMEOUT_* と表示される |

> <span style="color: #888888;">| 用例编号 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |</span>
> <span style="color: #888888;">|---|---|---|---|---|</span>
> <span style="color: #888888;">| TMO-01 | 超时转派 | 节点配置 ESCALATE + 目标用户 | 等待 timeoutTime 过期 | 自动转派给 escalateToUserId |</span>
> <span style="color: #888888;">| TMO-02 | 超时自动通过 | 节点配置 AUTO_APPROVE | 等待 timeoutTime 过期 | 自动通过，跳过并行任务，推进 |</span>
> <span style="color: #888888;">| TMO-03 | 超时自动驳回 | 节点配置 AUTO_REJECT | 等待 timeoutTime 过期 | 自动驳回，记录 TIMEOUT_REJECT |</span>
> <span style="color: #888888;">| TMO-04 | 无超时配置 | 节点未配置超时 | 长期 PENDING | 不触发超时处理 |</span>
> <span style="color: #888888;">| TMO-05 | 超时记录 | 超时触发 | 查看审批记录 | 显示 approverId=0，action=TIMEOUT_* |</span>

### 3.8 条件分岐モジュール（P2）

> <span style="color: #888888;">### 3.8 条件分支模块（P2）</span>

| ケース番号 | テストシナリオ | 前提条件 | 操作手順 | 期待結果 |
|---|---|---|---|---|
| CND-01 | 条件成立 | ノード条件 `days > 3` | 5日間の休暇を提出 | そのノードがトリガーされる |
| CND-02 | 条件不成立 | ノード条件 `days > 3` | 2日間の休暇を提出 | そのノードがスキップされる |
| CND-03 | タイプ条件 | ノード条件 `leaveType == '病假'` | 病欠を提出 | そのノードがトリガーされる |

> <span style="color: #888888;">| 用例编号 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |</span>
> <span style="color: #888888;">|---|---|---|---|---|</span>
> <span style="color: #888888;">| CND-01 | 条件满足 | 节点条件 `days > 3` | 提交 5 天请假 | 触发该节点 |</span>
> <span style="color: #888888;">| CND-02 | 条件不满足 | 节点条件 `days > 3` | 提交 2 天请假 | 跳过该节点 |</span>
> <span style="color: #888888;">| CND-03 | 类型条件 | 节点条件 `leaveType == '病假'` | 提交病假 | 触发该节点 |</span>

### 3.9 統計モジュール

> <span style="color: #888888;">### 3.9 统计模块</span>

| ケース番号 | テストシナリオ | 前提条件 | 操作手順 | 期待結果 |
|---|---|---|---|---|
| STA-01 | 統計を表示 | 承認データあり | 統計レポートに入る | 棒グラフ+円グラフが正常に表示される |
| STA-02 | データなし統計 | 承認記録なし | 統計レポートに入る | グラフが 0 または空で表示される |

> <span style="color: #888888;">| 用例编号 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |</span>
> <span style="color: #888888;">|---|---|---|---|---|</span>
> <span style="color: #888888;">| STA-01 | 查看统计 | 有审批数据 | 进入统计报表 | 柱状图+饼图正常显示 |</span>
> <span style="color: #888888;">| STA-02 | 空数据统计 | 无审批记录 | 进入统计报表 | 图表显示 0 或空 |</span>

### 3.10 エクスポートモジュール

> <span style="color: #888888;">### 3.10 导出模块</span>

| ケース番号 | テストシナリオ | 前提条件 | 操作手順 | 期待結果 |
|---|---|---|---|---|
| EXP-01 | Excel エクスポート | admin でログイン | Excel エクスポートをクリック | .xlsx ファイルがダウンロードされる |
| EXP-02 | 非管理者エクスポート | zhangsan でログイン | — | エクスポートボタンが表示されない |

> <span style="color: #888888;">| 用例编号 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |</span>
> <span style="color: #888888;">|---|---|---|---|---|</span>
> <span style="color: #888888;">| EXP-01 | 导出 Excel | admin 登录 | 点击导出 Excel | 下载 .xlsx 文件 |</span>
> <span style="color: #888888;">| EXP-02 | 非管理员导出 | zhangsan 登录 | — | 导出按钮不显示 |</span>

---

## 4. 承認フロー全体シナリオ

> <span style="color: #888888;">## 4. 审批流转完整场景</span>

### シーン1：SINGLE 正常承認通過

> <span style="color: #888888;">### 场景一：SINGLE 正常审批通过</span>

```
zhangsan が休暇申請を提出
  → ステータス PENDING、currentApproverId = admin（直属上司）
  → admin が APPROVE で承認
    → ステータス PENDING、currentApproverId = zongjian1（部署ディレクター）
    → zongjian1 が APPROVE で承認
      → ステータス APPROVED、フロー終了
```

> <span style="color: #888888;">```
> zhangsan 提交请假申请
>   → 状态 PENDING，currentApproverId = admin（直属领导）
>   → admin 审批 APPROVE
>     → 状态 PENDING，currentApproverId = zongjian1（部门总监）
>     → zongjian1 审批 APPROVE
>       → 状态 APPROVED，流程结束
> ```</span>

### シーン2：途中却下

> <span style="color: #888888;">### 场景二：中途驳回</span>

```
zhangsan が休暇申請を提出
  → admin が REJECT で承認
    → ステータス REJECTED、フロー終了
```

> <span style="color: #888888;">```
> zhangsan 提交请假申请
>   → admin 审批 REJECT
>     → 状态 REJECTED，流程终止
> ```</span>

### シーン3：撤回

> <span style="color: #888888;">### 场景三：撤回</span>

```
zhangsan が休暇申請を提出
  → zhangsan が撤回
    → ステータス WITHDRAWN
```

> <span style="color: #888888;">```
> zhangsan 提交请假申请
>   → zhangsan 撤回
>     → 状态 WITHDRAWN
> ```</span>

### シーン4：転送後通過

> <span style="color: #888888;">### 场景四：转派后通过</span>

```
zhangsan が休暇申請を提出
  → admin（直属上司）が zongjian2 に転送
    → ステータス PENDING、currentApproverId = zongjian2
    → zongjian2 が APPROVE で承認
      → ステータス PENDING、currentApproverId = zongjian1（部署ディレクター）
      → zongjian1 が APPROVE で承認
        → ステータス APPROVED
```

> <span style="color: #888888;">```
> zhangsan 提交请假申请
>   → admin（直属领导）转派给 zongjian2
>     → 状态 PENDING，currentApproverId = zongjian2
>     → zongjian2 审批 APPROVE
>       → 状态 PENDING，currentApproverId = zongjian1（部门总监）
>       → zongjian1 审批 APPROVE
>         → 状态 APPROVED
> ```</span>

### シーン5：会签全員通過（P2）

> <span style="color: #888888;">### 场景五：会签全部通过（P2）</span>

```
zhangsan が休暇申請を提出（会签ノードあり：admin + zongjian1）
  → approval_task × 2 を作成（admin PENDING, zongjian1 PENDING）
  → admin が APPROVE で承認 → admin タスク COMPLETED
    → ステータスは PENDING のまま（zongjian1 未完了）
  → zongjian1 が APPROVE で承認 → zongjian1 タスク COMPLETED
    → 全員完了、次のノードに遷移
```

> <span style="color: #888888;">```
> zhangsan 提交请假申请（含会签节点：admin + zongjian1）
>   → 创建 approval_task × 2（admin PENDING, zongjian1 PENDING）
>   → admin 审批 APPROVE → admin 任务 COMPLETED
>     → 状态仍 PENDING（zongjian1 未完成）
>   → zongjian1 审批 APPROVE → zongjian1 任务 COMPLETED
>     → 全部完成，推进到下一节点
> ```</span>

### シーン6：或签迅速通過（P2）

> <span style="color: #888888;">### 场景六：或签快速通过（P2）</span>

```
zhangsan が休暇申請を提出（或签ノードあり：admin + zongjian1）
  → approval_task × 2 を作成（admin PENDING, zongjian1 PENDING）
  → admin が APPROVE で承認 → admin タスク COMPLETED、zongjian1 タスク SKIPPED
    → 即座に次のノードに遷移
```

> <span style="color: #888888;">```
> zhangsan 提交请假申请（含或签节点：admin + zongjian1）
>   → 创建 approval_task × 2（admin PENDING, zongjian1 PENDING）
>   → admin 审批 APPROVE → admin 任务 COMPLETED，zongjian1 任务 SKIPPED
>     → 立即推进到下一节点
> ```</span>

### シーン7：条件分岐ノードスキップ（P2）

> <span style="color: #888888;">### 场景七：条件分支跳过节点（P2）</span>

```
zhangsan が2日間の年次休暇を提出（条件ノード：days > 3）
  → 条件不成立 → そのノードをスキップ → 直接次のノードへ
```

> <span style="color: #888888;">```
> zhangsan 提交 2 天年假（条件节点：days > 3）
>   → 条件不满足 → 跳过该节点 → 直接进入下一节点
> ```</span>

### シーン8：タイムアウト自動エスカレーション（P2）

> <span style="color: #888888;">### 场景八：超时自动升级（P2）</span>

```
zhangsan が休暇申請を提出（ノード設定：1h + AUTO_APPROVE）
  → timeoutTime = now + 1h
  → 1h 後に TimeoutScheduler がタイムアウトをスキャン
    → 自動通過 → 次のノードに遷移
```

> <span style="color: #888888;">```
> zhangsan 提交请假申请（节点配置 1h + AUTO_APPROVE）
>   → timeoutTime = now + 1h
>   → 1h 后 TimeoutScheduler 扫描到超时
>     → 自动通过 → 推进到下一节点
> ```</span>

---

## 5. APIテスト（curl サンプル）

> <span style="color: #888888;">## 5. 接口测试（curl 示例）</span>

### 5.1 ログインして Token を取得

> <span style="color: #888888;">### 5.1 登录获取 Token</span>

```bash
curl -X POST http://localhost:8080/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"zhangsan","password":"123456"}'
```

### 5.2 申請提出

> <span style="color: #888888;">### 5.2 提交申请</span>

```bash
curl -X POST http://localhost:8080/api/leave/submit \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"templateId":1,"leaveType":"年假","startDate":"2026-06-01","endDate":"2026-06-03","reason":"test"}'
```

### 5.3 承認（通過）

> <span style="color: #888888;">### 5.3 审批通过</span>

```bash
curl -X POST http://localhost:8080/api/leave/approve \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"requestId":1,"action":"APPROVE","comment":"同意"}'
```

### 5.4 撤回

> <span style="color: #888888;">### 5.4 撤回</span>

```bash
curl -X POST http://localhost:8080/api/leave/1/withdraw \
  -H "Authorization: Bearer <token>"
```

### 5.5 転送

> <span style="color: #888888;">### 5.5 转派</span>

```bash
curl -X POST http://localhost:8080/api/leave/1/transfer \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"toUserId":3}'
```

### 5.6 並列タスククエリ（P2）

> <span style="color: #888888;">### 5.6 并行任务查询（P2）</span>

```bash
curl -X GET http://localhost:8080/api/leave/1/tasks \
  -H "Authorization: Bearer <token>"
```

### 5.7 滞留修復（P2）

> <span style="color: #888888;">### 5.7 滞留修复（P2）</span>

```bash
curl -X POST http://localhost:8080/api/leave/repair \
  -H "Authorization: Bearer <token>"
```

---

## 6. 回帰テストチェックリスト

> <span style="color: #888888;">## 6. 回归测试检查清单</span>

- [ ] ログイン/ログアウト正常
- [ ] PENDING → APPROVE → 次のノードへ遷移（SINGLE）
- [ ] PENDING → REJECT → フロー終了
- [ ] PENDING → WITHDRAW → ステータス変更
- [ ] PENDING → TRANSFER → 承認者変更
- [ ] 会签：全員同意後に遷移
- [ ] 会签：誰かが拒否後に終了
- [ ] 或签：いずれかが同意後に遷移
- [ ] 並列ノード撤回：全タスク SKIPPED
- [ ] 条件分岐：成立/不成立で正しく分岐
- [ ] タイムアウト ESCALATE 転送
- [ ] タイムアウト AUTO_APPROVE 自動通過
- [ ] タイムアウト AUTO_REJECT 自動却下
- [ ] テンプレート CRUD + ノード設定（承認モード+条件+タイムアウト含む）正常
- [ ] 統計グラフ正常読み込み
- [ ] Excel エクスポートダウンロード可能
- [ ] 権限チェック有効（一般ユーザーはテンプレート管理不可）
- [ ] 滞留修復正常

> <span style="color: #888888;">- [ ] 登录/登出正常</span>
> <span style="color: #888888;">- [ ] PENDING → APPROVE → 流转下一节点（SINGLE）</span>
> <span style="color: #888888;">- [ ] PENDING → REJECT → 流程终止</span>
> <span style="color: #888888;">- [ ] PENDING → WITHDRAW → 状态变更</span>
> <span style="color: #888888;">- [ ] PENDING → TRANSFER → 审批人变更</span>
> <span style="color: #888888;">- [ ] 会签：全部同意后推进</span>
> <span style="color: #888888;">- [ ] 会签：有人拒绝后终止</span>
> <span style="color: #888888;">- [ ] 或签：任一人同意后推进</span>
> <span style="color: #888888;">- [ ] 并行节点撤回：所有任务 SKIPPED</span>
> <span style="color: #888888;">- [ ] 条件分支：满足/不满足正确分流</span>
> <span style="color: #888888;">- [ ] 超时 ESCALATE 转派</span>
> <span style="color: #888888;">- [ ] 超时 AUTO_APPROVE 自动通过</span>
> <span style="color: #888888;">- [ ] 超时 AUTO_REJECT 自动驳回</span>
> <span style="color: #888888;">- [ ] 模板 CRUD + 节点配置（含签批模式+条件+超时）正常</span>
> <span style="color: #888888;">- [ ] 统计图表正常加载</span>
> <span style="color: #888888;">- [ ] Excel 导出可下载</span>
> <span style="color: #888888;">- [ ] 权限校验生效（普通用户不能管理模板）</span>
> <span style="color: #888888;">- [ ] 滞留修复正常</span>

---

> ドキュメントバージョン：v2.0（P2 完了） | 更新日：2026-05-27

> <span style="color: #888888;">> 文档版本：v2.0（P2 完成） | 更新日期：2026-05-27</span>