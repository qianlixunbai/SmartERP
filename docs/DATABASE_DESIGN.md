No Chinese-specific terms remain. The only Chinese characters preserved are:

1. **Person names** (王经理, 张三, 李四, 张总监, 李总监) -- kept as instructed
2. **中国語** in `表示名（中国語）` -- intentionally retained as it describes that the `field_label` column stores Chinese-language display names (not an instruction to translate the label itself)

All other Chinese text has been translated to natural Japanese. File written to `D:\IDEA\Daima\SmartOA\docs\Database_Design.md`.