The complete bilingual file has been written to:

**`D:\IDEA\Daima\SmartOA\46-简易 OA 审批流管理系统_bilingual.md`**

Here is the complete content of the translated file:

---

```
# 選題標定 · 第 46 題：簡易 OA 承認フロー管理システム

> <span style="color: #888888;"># 选题标定 · 第 46 题：简易 OA 审批流管理系统</span>

> ⚠️ 本ファイルはあなたの選題標定カード · 単題専用版です
>
> **使い方**：
> 1. 本ファイルを `00-选题标定.md` にリネームします
> 2. プロジェクト作業ディレクトリの `docs/` に移動します
> 3. 以降、`@srs-writer` `@db-designer` などの Agent がこのファイルを P0/P1/P2 + 定量アンカーの権威ソースとして自動的に読み取ります

> <span style="color: #888888;">> ⚠️ 本文件是你的选题标定卡 · 单题专属版
> >
> > **怎么用**：
> > 1. 把本文件改名为 `00-选题标定.md`
> > 2. 移动到你项目工作目录的 `docs/` 下
> > 3. 后续 `@srs-writer` `@db-designer` 等 Agent 会自动读这个文件作为 P0/P1/P2 + 量化锚点的权威源</span>

---

### § 一 基本情報

> <span style="color: #888888;">### § 一 基本信息</span>

| 项 | 值 |
|---|---|
| 题号 | 46 |
| 题名 | 简易 OA 审批流管理系统 |
| 一句话背景 | 企业级业务复杂场景；核心是「模板配置 + 多级审批 + 流程引擎」三件事；**P0 严格锚定 14h，硬编码 2 级审批**，可配置引擎推到 P1，可视化编辑器推到 P2 |
| 核心实体 | `user` / `approval_template` / `approval_request` / `approval_log` |
| JWT 角色 | 发起人 / 审批人 / 管理员（user.role 字段） |
| 技术栈 | SpringBoot 3 + Vue 3 + MySQL 8 + MyBatis-Plus + Element Plus |

### § 二 定量アンカー

> <span style="color: #888888;">### § 二 量化锚点</span>

| 档位 | 表数 | 接口 | 页面 | 工时 |
|---|:-:|:-:|:-:|:-:|
| P0 | 4 | 约 10 | 6 | 14 h |
| P1 | 6 | 15-25 | 10-15 | 28 h |
| P2 | 6-7 | 25-30 | 15-22 | 48 h |

### § 三 P0 機能リスト（必須 · 60 点基準）

> <span style="color: #888888;">### § 三 P0 功能清单（必做 · 60 分基础）</span>

**エンティティ属性のヒント**：
- `approval_template` は「ハードコードされた 2 段階承認者フィールド」を含む（動的設定は非対応）
- `approval_request` は「申請データ」フィールドを含む
- `approval_log` は「承認ログ」フィールドを含む

> <span style="color: #888888;">**实体属性提示**：
> - `approval_template` 含「硬编码 2 级审批人字段」（不支持动态配置）
> - `approval_request` 含「申请数据」字段
> - `approval_log` 含「审批日志」字段</span>

**⚠️ P0 の重要な簡略化**：承認フローノードは**ハードコード** —— 例えば「休暇申請」テンプレートは "直属上司 → 部門ディレクター → アーカイブ" と固定で流れます；コード内に `if step == 1 → approver = directLeader; if step == 2 → approver = departmentHead;` と書き込みます。**学生は P0 を完了すれば 60 点**となり、第 1 題の商品 CRUD と同じ作業量感覚です（業務複雑度の違いは採点に影響しません）。

> <span style="color: #888888;">**⚠️ P0 关键简化**：审批流程节点**硬编码** —— 例如"请假申请"模板固定流转 "直属领导 → 部门总监 → 归档"；代码里写死 `if step == 1 → approver = directLeader; if step == 2 → approver = departmentHead;`。**学生做完 P0 即 60 分**，与第 1 题商品 CRUD 工作量感受一致（业务复杂度差异不影响评分）。</span>

**機能リスト（約 10 インターフェース）**：
1. **ログイン / JWT**（約 2 インターフェース）
2. **承認テンプレート CRUD**（約 4 インターフェース · 固定 2 段階のみ、動的設定は非対応）
3. **申請提出**（1 インターフェース）
4. **ハードコード 2 段階承認フロー**（1 インターフェース · コード内に固定）
5. **自分の未処理 / 自分の提出**（約 2 インターフェース）

> <span style="color: #888888;">**功能清单（约 10 接口）**：
> 1. **登录 / JWT**（约 2 接口）
> 2. **审批模板 CRUD**（约 4 接口 · 只做固定 2 级，不支持动态配置）
> 3. **申请提交**（1 接口）
> 4. **硬编码 2 级审批流转**（1 接口 · 代码里写死）
> 5. **我的待办 / 我的提交**（约 2 接口）</span>

**ページ 6 つ**：ログイン / 承認テンプレート一覧（閲覧のみ、フローノード変更不可）/ テンプレート編集（基本情報のみ変更）/ 申請提出 / 自分の承認（タブ：未処理/処理済/自分が提出）/ 承認詳細

> <span style="color: #888888;">**页面 6 个**：登录 / 审批模板列表（只能看不能改流程节点）/ 模板编辑（只改基本信息）/ 提交申请 / 我的审批（Tab：待办/已办/我提交）/ 审批详情</span>

### § 四 P1 推奨リスト（70-80 点 · 累計 28 h）

> <span style="color: #888888;">### § 四 P1 应做清单（70-80 分 · 累计 28 h）</span>

- **データベース**：+ `approval_node`（承認ノードテーブル、独立化）+ `template_field`（テンプレートフォームフィールドテーブル）= 6 テーブル
- **バックエンド**：設定可能な承認エンジン（ノードテーブル + 順序フィールド + 実行時動的テンプレートフロー読み取り）+ 承認/却下/撤回/転送 の 4 アクション + 承認コメント + Excel エクスポート
- **フロントエンド**：テンプレート編集のアップグレード（任意段階数の承認ノード設定）+ フロー進捗バー（`el-steps`）+ 承認履歴タイムライン + ECharts 1 つ（平均承認時間/各テンプレート使用量）
- **権限**：申請者 / 承認者 / 管理者 の 3 ロール

> <span style="color: #888888;">- **数据库**：+ `approval_node`（审批节点表，独立出来）+ `template_field`（模板表单字段表）= 6 张
> - **后端**：可配置审批引擎（节点表 + 顺序字段 + 运行时动态读取模板流转）+ 同意/拒绝/撤回/转派 4 动作 + 审批意见 + 导出 Excel
> - **前端**：模板编辑升级（任意级数审批节点配置）+ 流程进度条（`el-steps`）+ 审批历史时间线 + 1 个 ECharts（平均审批时长/各模板使用量）
> - **权限**：发起人 / 审批人 / 管理员 3 角色</span>

### § 五 P2 オプションリスト（85+ · 累計 48 h · 選択 · 強制なし）

> <span style="color: #888888;">### § 五 P2 可选清单（85+ · 累计 48 h · 选做不强求）</span>

- **フローノードビジュアルエディタ**（ドラッグ＆ドロップでノード配置 + 接続線 + 条件分岐 · Vue Flow または JSPlumb）
- **承認条件分岐**（例：「金額 > 10000 の場合、自動的に財務承認を追加」）+ **並列承認ノード**（複数人同時/いずれかが通過）+ **CC メカニズム**
- **オプション加点**：タイムアウト自動エスカレーション（`@Scheduled`）、電子署名、PDF エクスポート（Apache PDFBox）、単体テスト +4

> <span style="color: #888888;">- **流程节点可视化编辑器**（拖拽节点+连线+条件分支 · Vue Flow 或 JSPlumb）
> - **审批条件分支**（如"金额>10000 自动加财务审批"）+ **并行审批节点**（多人同时/任一通过）+ **抄送人机制**
> - **可选加分**：超时自动升级（`@Scheduled`）、电子签名、PDF 导出（Apache PDFBox）、单测 +4</span>
```