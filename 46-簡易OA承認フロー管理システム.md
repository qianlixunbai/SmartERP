# 課題標定 · 第46題：簡易OA承認フロー管理システム

⚠️ 本ファイルはあなたの課題標定カード · 単題専用版です
**使い方**：
1. 本ファイルを `00-課題標定.md` にリネームします
2. プロジェクト作業ディレクトリの `docs/` 配下に移動します
3. 以降、`@srs-writer` `@db-designer` などのAgentがこのファイルをP0/P1/P2 + 定量アンカーの権威ソースとして自動的に読み取ります

---

### § 一 基本情報

### § 一 基本情報

| 項目 | 値 |
|---|---|
| 題番号 | 46 |
| 題名 | 簡易OA承認フロー管理システム |
| 一言背景 | エンタープライズ級の複雑な業務シナリオ；コアは「テンプレート設定 + 多段階承認 + フローエンジン」の3要素；**P0は厳密に14hにアンカーし、2段階承認をハードコード**、設定可能エンジンはP1に、ビジュアルエディタはP2に繰り延べ |
| コアエンティティ | `user` / `approval_template` / `approval_request` / `approval_log` |
| JWTロール | 申請者 / 承認者 / 管理者（user.roleフィールド） |
| 技術スタック | SpringBoot 3 + Vue 3 + MySQL 8 + MyBatis-Plus + Element Plus |

### § 二 定量アンカー

| レベル | テーブル数 | API数 | ページ数 | 工数 |
|---|:-:|:-:|:-:|:-:|
| P0 | 4 | 約10 | 6 | 14 h |
| P1 | 6 | 15-25 | 10-15 | 28 h |
| P2 | 6-7 | 25-30 | 15-22 | 48 h |

### § 三 P0機能一覧（必須 · 60点基礎）

**エンティティ属性ヒント**：
- `approval_template` は「ハードコードされた2段階承認者フィールド」を含む（動的設定非対応）
- `approval_request` は「申請データ」フィールドを含む
- `approval_log` は「承認ログ」フィールドを含む

**⚠️ P0の重要な簡略化**：承認フローノードは**ハードコード** —— 例：「休暇申請」テンプレートは固定フロー「直属上司 → 部門長 → アーカイブ」；コード内に `if step == 1 → approver = directLeader; if step == 2 → approver = departmentHead;` と記述。**学生はP0を完了すれば60点**、第1題の商品CRUDと同じ作業量感覚（業務複雑度の差は評価に影響しない）。

**機能一覧（約10API）**：
1. **ログイン / JWT**（約2API）
2. **承認テンプレートCRUD**（約4API · 固定2段階のみ、動的設定非対応）
3. **申請提出**（1API）
4. **ハードコードされた2段階承認フロー**（1API · コード内に記述）
5. **自分の待機案件 / 自分の提出案件**（約2API）

**ページ6つ**：ログイン / 承認テンプレート一覧（閲覧のみ、フローノード変更不可）/ テンプレート編集（基本情報のみ変更）/ 申請提出 / 自分の承認（タブ：待機案件/処理済み/提出案件）/ 承認詳細

### § 四 P1実施推奨一覧（70-80点 · 累計28h）

- **データベース**：+ `approval_node`（承認ノードテーブル、独立化）+ `template_field`（テンプレートフォームフィールドテーブル）= 6テーブル
- **バックエンド**：設定可能な承認エンジン（ノードテーブル + 順序フィールド + 実行時動的テンプレートフロー読み取り）+ 承認/却下/取消/転送の4アクション + 承認コメント + Excelエクスポート
- **フロントエンド**：テンプレート編集強化（任意段階の承認ノード設定）+ フロー進捗バー（`el-steps`）+ 承認履歴タイムライン + 1つのECharts（平均承認時間/テンプレート別使用量）
- **権限**：申請者 / 承認者 / 管理者の3ロール

### § 五 P2オプション一覧（85点以上 · 累計48h · 選択制、必須ではない）

- **フローノードビジュアルエディタ**（ノードのドラッグ+接続+条件分岐 · Vue FlowまたはJSPlumb）
- **承認条件分岐**（例：「金額>10000で自動的に財務承認を追加」）+ **並列承認ノード**（複数人同時/いずれか通過）+ **CCメカニズム**
- **オプション加点**：タイムアウト自動エスカレーション（`@Scheduled`）、電子署名、PDFエクスポート（Apache PDFBox）、単体テスト +4